package org.example.chapter7_arrays;
import java.util.Scanner;
/*Make an array that holds its actual values of the days of the week,
and then have the user input a number corresponding to some day of the week.
And assume that the week starts on Monday.
Your program should output the String that represents the day of the week that
corresponds to the number that the user input. For example, if the user inputs the number 1,
your program should print “Monday”.*/

/* input: numbers from 1-5
   process:
   output: string that represents the day of the week*/

public class DayOfWeek_arraysExercise {
    public static void main(String[] args) {
        /*// variables we know
        String[] weekArray = {"Monday","tuesday", "wednesday", "thursday","friday"};
        // variables we don't know
        System.out.println("Enter the number for the day of the week between 1-5");
        Scanner scanner= new Scanner(System.in);
        int inputIndex = scanner.nextInt();
        scanner.close();
        //output
        String userDay = weekArray[inputIndex -1];
        System.out.println("The number you enter correspond to the day: "+ userDay+ "!!");*/

        String [] daysWeek = {"monday", "tuesday", "wednesday","thursday", "friday"};
        String newString = "tree";
        StringBuilder newS = new StringBuilder(newString);
        



    }
}
